//DOM-selector for the canvas 👇
const ctx = document.getElementById('chart').getContext('2d')

//"Draw" the chart here 👇
const myChart = () => {
    const config = {
        type: 'doughnut',
        backgroundColor: [
            '#1400D1',
            '#CEE6FF'
        ]
        
    }
}
